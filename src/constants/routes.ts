export enum RoutePaths {
    HOME = "/",
    FINISHED = "/finished",
    SEARCH = "/search",
    STATS = "/stats",
    SETTINGS = "/settings",
    BOOK = "/book",
    AUTHOR = "/author",
    NEW = "/new",
}
